pull_request_rules:
  - name: Auto-merge PRs when all checks pass
    conditions:
      - base=main
      - check-success=test
    actions:
      merge:
        method: squash
        commit_message_template: |
          {{ title }}

          {{ body }}
      # Automatically clean up the branch after merging
      delete_head_branch: {}
