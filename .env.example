# Server-side only — set in the Vercel project settings, never commit real values.
# Used by app/api/account/route.ts to delete a Supabase Auth user on request.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
